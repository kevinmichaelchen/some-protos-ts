# Ignore JetBrains editor stuff
.idea

# Ignore the Protobuf monorepo. We can clone it during the build, but we never want to check it in.
some-protos

# Ignore the .bin folder where we the build stores a symlink for the TypeScript proto plugin
.bin